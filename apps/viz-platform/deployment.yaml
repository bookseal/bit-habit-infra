apiVersion: apps/v1
kind: Deployment
metadata:
  name: viz-platform
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: viz-platform
  template:
    metadata:
      labels:
        app: viz-platform
    spec:
      containers:
        - name: viz-platform
          # 주의: 이미지 이름은 앱 이름(viz-platform)이 아니라 viz-bit-habit 입니다
          image: localhost:5000/viz-bit-habit:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8501
          volumeMounts:
            - name: src
              mountPath: /app
      volumes:
        - name: src
          hostPath:
            path: /home/ubuntu/workspace/linear-algebra-visualization
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: viz-platform-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: viz-platform
  ports:
    - port: 80
      targetPort: 8501
