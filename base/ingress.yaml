# =============================================================================
# base/ingress.yaml  –  Main Ingress routing for all bit-habit.com subdomains
#
# This single Ingress resource maps every public hostname to the correct
# in-cluster Service.  Traefik (bundled with k3s) reads these rules and
# forwards HTTPS traffic accordingly.
#
# TLS certificates are provisioned by the explicit Certificate resource in
# base/cert-manager/certificate.yaml (cert-manager + ClusterIssuer
# "letsencrypt-prod"). The cert is stored in the Secret named "tls-secret"
# and referenced via spec.tls.secretName below.
#
# IMPORTANT: do NOT add a `cert-manager.io/cluster-issuer` annotation here.
# That annotation makes cert-manager's ingress-shim auto-create a SECOND
# Certificate (named after the secret, "tls-secret") that fights the explicit
# one above for the same Secret, leaving one stuck Ready=False
# (reason: IncorrectCertificate). Manage the cert only via certificate.yaml.
# =============================================================================

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik # Use Traefik as the ingress controller
    traefik.ingress.kubernetes.io/router.entrypoints: websecure # Accept HTTPS traffic only (port 443)
    traefik.ingress.kubernetes.io/router.tls: "true" # Enable TLS termination at Traefik
    # NOTE: cert-manager.io/cluster-issuer intentionally omitted — see header comment.
    # The TLS cert is owned by the explicit Certificate in base/cert-manager/certificate.yaml.
spec:
  tls:
    - hosts:
        - bit-habit.com
        - "*.bit-habit.com"
      secretName: tls-secret # Wildcard certificate stored in this Secret
  rules:
    # ---- Main landing page ----
    - host: bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: static-web-svc
                port:
                  number: 80
    - host: www.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: static-web-svc
                port:
                  number: 80
    # ---- Ghost blog (blog.bit-habit.com) ----
    - host: blog.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ghost-svc
                port:
                  number: 80
    - host: www.blog.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ghost-svc
                port:
                  number: 80
    # ---- Booktoss – book-sharing service ----
    - host: booktoss.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: booktoss-svc
                port:
                  number: 80
    # ---- Code-server – web IDE (host systemd service) ----
    - host: code-server.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: code-server-svc
                port:
                  number: 80
    # ---- Daily Seongsu – Gradio data app ----
    - host: daily-seongsu.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: daily-seongsu-svc
                port:
                  number: 80
    # ---- Habit tracker (habit.bit-habit.com) ----
    # Deliberately NOT here. Both halves of this host live next to their app:
    #   frontend  ->  apps/bithabit-web/ingress.yaml
    #   /api/*    ->  apps/bithabit-api/ingress.yaml
    # ---- Startpage – personal dashboard ----
    - host: startpage.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: startpage-svc
                port:
                  number: 80
    # ---- Status page (static site) ----
    - host: status.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gatus-svc
                port:
                  number: 80
    # ---- Sentinel – real-time cognitive assistant ----
    - host: sentinel.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sentinel-svc
                port:
                  number: 80
    # ---- Seoul Apartment Price app ----
    - host: seoul-apt.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: seoul-apt-price
                port:
                  number: 80
    # ---- Viz Platform – data visualisation ----
    - host: viz.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: viz-platform-svc
                port:
                  number: 80
    # ---- Wiki.js – internal knowledge base ----
    - host: wiki.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wikijs-svc
                port:
                  number: 80
    - host: www.wiki.bit-habit.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wikijs-svc
                port:
                  number: 80
