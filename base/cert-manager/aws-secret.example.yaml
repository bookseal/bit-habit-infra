# TEMPLATE — copy to aws-secret.yaml and fill in real Route53 credentials.
# The real aws-secret.yaml is gitignored; never commit it. Rotate keys if leaked.
#
apiVersion: v1
kind: Secret
metadata:
  name: route53-credentials-secret
  namespace: cert-manager
type: Opaque
stringData:
  secret-access-key: "REPLACE_ME"
